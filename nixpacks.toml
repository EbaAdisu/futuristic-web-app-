[build]
# Specify the build environment and commands
builder = "nodejs"

# Define the build command
command = "npm install && pnpm build"

[run]
# Specify the runtime environment and commands
command = "npm start"

[static]
# Define static files to serve (if applicable)
path = "public"
